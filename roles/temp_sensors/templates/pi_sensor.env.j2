# Generated by mri-cooling-pi-fleet

SITE_NAME={{ temp_sensors.site_name | default('GMC') }}
SCANNER_ID={{ temp_sensors.scanner_id | default('') }}

BACKEND_URL={{ temp_sensors.backend_url }}
ALERTER_METRICS_URL={{ temp_sensors.alerter_metrics_url | default('') }}

DEV_BACKEND_URL={{ temp_sensors.dev_backend_url | default('') }}
ENABLE_DUAL_STREAMING={{ (temp_sensors.enable_dual_streaming | default(false)) | ternary('true','false') }}

MEMORY_ONLY_MODE={{ (temp_sensors.memory_only_mode | default(false)) | ternary('true','false') }}
WEAR_LEVELING={{ (temp_sensors.wear_leveling | default(true)) | ternary('true','false') }}
RAM_LOG_CAPACITY={{ temp_sensors.ram_log_capacity | default(1000) }}
DISK_SYNC_INTERVAL={{ temp_sensors.disk_sync_interval | default(3600) }}

PROBE_IN_ID={{ temp_sensors.probe_in_id }}
PROBE_OUT_ID={{ temp_sensors.probe_out_id }}
PROBE_PRIMARY_IN_ID={{ temp_sensors.probe_primary_in_id }}
PROBE_PRIMARY_OUT_ID={{ temp_sensors.probe_primary_out_id }}
PROBE_ROOM_ID={{ temp_sensors.probe_room_id | default('') }}

AVG_WINDOW={{ temp_sensors.avg_window | default(5) }}
POLL_SEC={{ temp_sensors.poll_sec | default(10) }}
INVALID_SENSOR_RESTART_THRESHOLD={{ temp_sensors.invalid_sensor_restart_threshold | default(3) }}
SENSOR_DISCOVERY_TIMEOUT_SEC={{ temp_sensors.sensor_discovery_timeout_sec | default(90) }}
SENSOR_DISCOVERY_INTERVAL_SEC={{ temp_sensors.sensor_discovery_interval_sec | default(5) }}
SENSOR_RESCAN_TIMEOUT_SEC={{ temp_sensors.sensor_rescan_timeout_sec | default(20) }}
SENSOR_RESCAN_INTERVAL_SEC={{ temp_sensors.sensor_rescan_interval_sec | default(5) }}

LOG_PATH={{ temp_sensors.log_path | default('logs/pi_sensor.log') }}

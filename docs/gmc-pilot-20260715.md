# GMC MR1–3 pilot record — 2026-07-15

## Scope

The nearby GMC/AGC MR1–3 group is the controlled pilot group. Changes are made to
one host at a time, followed by service, sensor, and server-side telemetry checks.

## Sensor program result

- MR1 and MR3 were atomically updated to the approved MRI sensor program.
- MR2 already matched the approved program and was not restarted.
- MR1 rollback copy:
  `/home/agcmr1/mike-mri-cooling/src/pi_sensor.py.pre-fleet-20260715-200150`
- MR3 rollback copy:
  `/home/agcmr3/mike-mri-cooling/src/pi_sensor.py.pre-fleet-20260715-200319`
- Approved semantic SHA-256:
  `0d939f88b35ab8167ce1e70d91a8611a14f4a6a9631a30677bbe06bb5234a36f`

Post-change audit `audit_20260715_200637.json` confirmed:

| Host | Service | Approved code | RAM-only app log | Sensors | Server telemetry |
| --- | --- | --- | --- | ---: | --- |
| GMC MR1 | active | yes | yes | 5 | current / online |
| GMC MR2 | active | yes | yes | 4 | current / online |
| GMC MR3 | active | yes | yes | 5 | current / online |

No rollback was required. Sensor IDs, offsets, and environment settings were
preserved, and exact host definitions now live under `inventory/host_vars/`.

## MR1 journald pilot

The MR1 policy was applied on 2026-07-15 after a read-only state check. Immediate
post-change verification confirmed:

- effective `Storage=volatile` and `RuntimeMaxUse=32M`;
- a 4 MiB runtime journal under `/run/log/journal`;
- active journald, time synchronization, and MRI sensor services;
- five visible probes and the approved sensor hash;
- approximately 6.8 GiB memory still available; and
- current, online CoolMRI telemetry.

The existing approximately 2.3 GiB `/var/log/journal` archive is retained during
the observation period. It receives no new journald writes under the volatile
policy and can be vacuumed after the pilot is accepted. The rollback playbook
removes the drop-in and returns journald to the OS/default storage policy.

The full post-maintenance audit `audit_20260715_224228.json` still reports the MR1
policy effective, the sensor service active, five probes present, and no throttle
flags. MR2 and MR3 intentionally remain on persistent journald while MR1 completes
its observation period. Do not delete the retained archive or expand the policy
until troubleshooting quality and reboot behavior have been reviewed.

A read-only follow-up at 22:47 confirmed all three relevant services active,
five probes, live GPIO4 input state, `throttled=0x0`, and the effective 32 MiB
runtime cap. The retained `/var/log/journal` directory still has its historical
2025-04-15 directory timestamp. Because MR1 has not rebooted since the policy was
applied, post-reboot logging behavior remains an explicit pilot gate.

## Nearby package/reboot pilots

AGC MR3 and all three reachable GMC CV endpoints were used for the first controlled
package/reboot trials because they are physically close to the office. Updates and
reboots were serial, backup-gated, and followed by local service/probe checks plus
server-side telemetry verification. The complete record is in
`docs/package-maintenance-20260715.md`.

```sh
.venv/bin/ansible-playbook -i inventory/hosts.yml playbooks/edge_baseline.yml \
  --limit agcmr1 --check --diff -K
.venv/bin/ansible-playbook -i inventory/hosts.yml playbooks/edge_baseline.yml \
  --limit agcmr1 -K
.venv/bin/ansible-playbook -i inventory/hosts.yml playbooks/edge_baseline_rollback.yml \
  --limit agcmr1 -K
```

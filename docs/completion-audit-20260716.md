# Fleet objective completion audit — 2026-07-16

This audit tests the original objective against production evidence. “Achieved”
means current authoritative evidence covers every reachable host in scope;
“partial” means useful deployment exists but fleet coverage is incomplete;
“blocked” identifies the exact missing authority or field condition. A passing
unit test or prepared playbook is not counted as proof that a Pi changed.

Post-GR complete report `audit_20260718_125026.json` joins 23 reachable and four
offline inventory records with current backend, backup, package, runtime, and
optimization evidence. The portal continues replacing it with newer complete
15-minute audits rather than pinning a stale source.

| Original requirement | State | Authoritative evidence | Remaining work |
|---|---|---|---|
| Discover and centrally list the fleet | Partial | 27 inventory records; 23 reachable and four offline in the latest complete portal source | PHC, PHMV, VWM1, and GLH require field power/network recovery before current facts can be proven |
| Central management information at `pi.coolmri.com` | Achieved | Valid public TLS; unauthenticated root returns 401; authenticated API serves inventory, settings, backups, offsets, GPIO, health, upgrades, and recommendations. Public `/healthz` now returns 503 if complete fleet evidence is absent or older than 45 minutes. The portal separately reports installed sensor software environments and end-to-end sensor health, which requires exact physical probes, an active service, and current complete telemetry; this correctly exposes AGC MR2 as software-complete but not operational | Continue recurring maintenance; no write controls are intentionally exposed |
| Common operational package layer | Achieved for reachable fleet | 23/23 have all 13 required shared packages, use the same signed official stable channel, and run Tailscale 1.98.9. GR remains correctly on the supported Ubuntu 24.04 cohort while sharing the current cross-cohort package. Complete audit `audit_20260718_125026.json` proves the current state | Reassess offline devices when they return |
| Common update behavior | Achieved for reachable fleet | 23/23 have active/enabled apt timers, daily package refresh/upgrades, no held packages, and no automatic reboot | Reassess offline devices when they return |
| Profile-aware sensor runtime dependencies | Achieved for reachable fleet | 23/23 active service interpreters satisfy the common compatible dependency floors; MRI and CV remain separate profiles | Reassess offline devices when they return |
| Profile-aware services and crash recovery | Partial / field repair | 23/23 sensors run as site accounts, all service units align, and all 23 effective restart policies now match their profile. GR's metadata-only transaction retained its PID/start timestamp, application, five probes, and advancing telemetry. AGC MR2's service cannot finish startup because its physical 1-Wire bus exposes zero of four configured probes | Cold-power-cycle AGC MR2 and inspect its shared sensor-bus 3.3 V, ground, GPIO4 data connector, and pull-up |
| Approved MRI/CV sensor application | Achieved for reachable fleet | All three reachable CV hosts and all 20 reachable MRI hosts use their approved profile build. VWM2's final guarded transaction preserved its exact site files and five-probe set, recovered the service, and proved advancing telemetry at -73 dBm. Complete audit `audit_20260718_110912.json` reports zero software drift | Reassess offline devices when they return |
| Preserve site identity, probes, offsets, and GPIO | Partial / physical fault | All configured probe IDs, offsets, site identities, and GPIO mappings remain byte-for-byte preserved, and 22/23 reachable physical mappings are exact. AGC MR2 alone exposes zero of its four configured probes despite the preserved GPIO4 overlay and driver | Repair or cold-power-cycle AGC MR2's shared 1-Wire bus; reassess offline devices when they return; do not copy site settings between Pis |
| Safe software/package updating | Achieved for reachable fleet | All 23 reachable Pis have zero eligible packages, no reboot requirement, and Tailscale 1.98.9. Ubuntu lists six phased/deferred entries across the three GMC CV Pis, but normal APT offers none and the policy correctly does not force them. GR's boot image, offline clean FAT check, guarded update, controlled kernel reboot, two fresh backup/restore proofs, healthy SMART result, and independent audit cleared its former hold without changing its supported Ubuntu 24.04 OS exception | Let phased packages become normally eligible; offline Pis cannot be prepared until they return |
| Supported operating system convergence | Achieved for reachable fleet | 22 reachable Pis run Ubuntu 26.04 LTS; GR remains on supported Ubuntu 24.04 LTS by design. No reachable Pi remains on an end-of-life interim release. Every release wave has a restore-gated baseline, persistent local transaction, Python-runtime recovery, guarded reboot, strict 26-check comparison, and post-upgrade decrypt/restore proof. Shamokin safely followed 25.04→25.10→26.04; GCMC MR2 preserved its natural 10/10 camera session, exact camera software, schedule, five probes, and both Python runtimes. Weak Wi-Fi remained advisory during successful VWM2 and Jersey Shore waves | Reassess the four offline Pis when field connectivity returns; retain GR on its supported LTS exception until a separately justified move |
| Backups and restoration | Achieved for configured/reachable scope | The bounded four-worker production service ran from 15:50:12 through 16:22:33 EDT with result `success`, exit 0, and 24/24 atomic markers inside the exact service window. The exact follow-up sweep passed 24/24 latest snapshot-ID-matched decrypt-and-restore tests in tmpfs with zero failures. This reduced elapsed backup time from 72:49 serially to 32:21 while preserving per-repository isolation. Successful markers are now append-only as well as latest, so a targeted refresh cannot erase historical batch proof. MR1's corrected camera contract has a newer exact snapshot and restore while the full batch remains 24/24. The catalog includes sewer-pi; four missing fleet repositories map exactly to offline Pis | Keep the daily pull and Sunday restore-sweep timers monitored. Offline targets need first backups after field recovery |
| Reduce SSD writes with RAM | Partial / observing | Five volatile-journal canaries, two rsyslog-to-RAM pilots, four headless canaries, and three bounded camera RAM-log deployments are reboot-verified; durable retry state and historical camera data remain on SSD. All 23 reachable RAM budgets pass. Several Muncy audits were incomplete because its controller-side service account retained a stale host key; the strict observer correctly excluded them and restarted after the resulting 59-minute evidence gap | The clean canary window restarted at `2026-07-18T12:44:25-04:00`; earliest 48-hour gate is 2026-07-20 12:44 EDT, subject to 24 samples and three valid write-rate intervals |
| Reboot where needed without losing sensors | Partial / new field failure | Multiple controlled reboot reports prove recovery elsewhere, but AGC MR2's release-upgrade reboot exposed a shared physical 1-Wire bus failure. Kernel 7.0, retained 6.17, a driver reload, a known-ID read, a GPIO4 reset, and three additional operator-requested warm reboots all produced zero probes. The three attempts are recorded in `reboot_20260717_163035.json`, `reboot_20260717_163351.json`, and `reboot_20260717_163704.json` | Cold-power-cycle and inspect AGC MR2 locally; keep MR2 individually held while independently healthy hosts continue through their own guarded waves |
| Wi-Fi concerns and recommendations | Achieved for current evidence | Routed-interface signal, radio band, bitrate, adapter/driver, and power-save state feed per-host recommendations; weak secondary radios do not create false alerts or block otherwise safe work. Jersey Shore updated successfully and VWM2 completed sensor-code convergence around -73 dBm | Field improvement remains recommended for marginal sites: reposition first, then use an external-antenna USB adapter or Ethernet if they remain below -67 dBm |
| Camera-agent convergence | Partial | AGC MR1 and MR3 run the same managed v3.0.0 core and both passed exact-build natural 10/10 production proofs while preserving their distinct reviewed optics and upload contracts. OSW MR1 then passed the generic RAM-only, upload-disabled hardware canary and the guarded byte-exact-rollback production transaction. Its installed smoke captured one image and uploaded zero; schedules, five probes, sensor service, telemetry, throttling, and all four canonical hashes passed. A fresh encrypted post-deployment snapshot and real tmpfs restore also passed. The controller proof planner is armed for OSW MR1's natural 15:15 EDT ten-upload session. Nine secondary hosts now have RAM-only one-capture/zero-upload hardware-canary proof: OSW MR2 plus GCMC, GCMC MR2, GSWB, Jersey Shore, Muncy, Pittston Sola, Pittston Vida, and SVI MR2. Every production camera hash remained unchanged, and the portal holds all nine from deployment until OSW MR1's natural proof exists. All 13 installed camera runtimes remain dependency-complete, and AGC MR2's distinct local-OCR block remains protected | Require OSW MR1's armed 2026-07-18 15:15 natural 10/10 v3 proof before deploying any reviewed secondary; do not overwrite AGC MR2's local-OCR block |
| Recurring evidence and historical safety | Achieved | Enabled 15-minute systemd timer runs fleet audit, backup coverage, optimization observation, last-known indexing, and the read-only AGC MR2 recovery watcher. Muncy's stale controller known-host entry was fingerprint-verified and replaced; complete audit `audit_20260718_125026.json` again reports 23 online/four offline and publishes GR's final state. The strict observation restart demonstrates incomplete reports remain excluded from fleet-wide claims | Continue monitoring the restarted observation window |

## Completion decision

The original objective is not complete. Central management, reachable-host OS
convergence, shared packages/runtimes, backups, and recommendations are live,
but four offline Pis remain unverified, AGC MR2 still has a physical sensor-bus
failure, camera convergence remains deliberately staged, and the restarted
48-hour RAM/write observation has not matured.

Backup-hub administration, full-batch execution, and reachable-host restore
coverage are no longer blockers: all 24 configured targets completed in one
production service window and have current successful decrypt-and-restore proof.
AGC MR3's attended OS pilot is complete and verified. AGC MR2 reached the
target OS but remains explicitly failed until its four probes and telemetry are
restored; this is an individual host hold and no longer blocks independently
healthy Pis. GMC MR1, OSW MR1/MR2, GSWB, GBH, SVI MR1/MR2, Jersey Shore, VWM3, GWV, VWM2, GCMC, Pittston Sola, Muncy, and Pittston Vida subsequently
completed their own strict upgrade, reboot, and post-upgrade restore proofs.
Weak Wi-Fi is now advisory rather than a mutation blocker when every independent
hard gate passes; VWM2 proved that procedure at approximately -78 dBm.
